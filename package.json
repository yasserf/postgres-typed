{
  "name": "typed-postgres",
  "version": "0.0.7",
  "author": "Vlandor LTD",
  "license": "MIT",
  "main": "dist/src",
  "scripts": {
    "tsc": "rm -rf dist && tsc",
    "ncu": "ncu",
    "test": "echo 'No tests'",
    "test:ci": "echo 'No tests'",
    "example:schema": "schemats postgres postgres://postgres:password@localhost/schemats -s pet_store -o src/example/db-types.ts -f ./db-json-types -c -e"
  },
  "dependencies": {
    "@vramework/generic": "^0.0.0",
    "pg": "^8.7.1",
    "pg-camelcase": "^0.0.3",
    "pino": "^6.13.1",
    "snake-case": "^3.0.4"
  },
  "devDependencies": {
    "@types/pg": "^8.6.1",
    "@types/pino": "^6.3.11",
    "@vramework/schemats": "latest",
    "typescript": "^4.4.2"
  }
}
